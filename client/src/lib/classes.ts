// Shared class names used in multiple modules.
// `section-label` is defined globally because both sidebar and a couple of
// in-page panels use the same editorial caption style.
export const sectionLabelClass = 'section-label';
